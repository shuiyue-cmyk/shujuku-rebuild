/**
 * service/table/spv79-transition-checkpoint.ts — 再导出 shim
 *
 * 实现已迁入 compat-transition-checkpoint.ts（通用兼容过渡根与 spv79 专用根
 * 共存于同一模块）。保留本文件是为了不破坏既有导入路径。
 */
export * from './compat-transition-checkpoint';
